# Тестовый контейнер build-time-sender

при запуске контейнер отправляет сообщение с датой сборки в VK Teams

## setup
```bash
git clone test-container.git
cp .env.example .env
nano .env # Provide env vars in editor
run.sh 
```
